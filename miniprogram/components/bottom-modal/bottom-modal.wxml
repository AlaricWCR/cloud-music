<view class="modal" hidden="{{!modalShow}}">
  <view class="panel">
    <i class="iconfont icon-baseline-close-px" bind:tap="onClose"></i>
    <!-- slot插槽  聚名插槽 -->
    <slot name="modal-content"></slot>
  </view>
</view>